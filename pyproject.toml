[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"

[project]
name = "NTEUID"
version = "0.1.0"
description = "异环插件"
authors = [{name = "tyql88"}]
readme = "README.md"
requires-python = ">=3.11, <4.0"
dependencies = [
    "pycryptodome>=3.20.0",
]

[tool.pyright]
include = ["NTEUID"]
# scorers/ 是外置评分包(gitignore)，相对导入仅在运行时由 registry 挂载成立，静态检查排除
exclude = ["NTEUID/scoring/scorers"]
venvPath = "../../.."
venv = ".venv"
pythonVersion = "3.11"
typeCheckingMode = "basic"
